<form method="post" action="{{ path('app_cuentas_delete', {'id': cuenta.id}) }}" onsubmit="return confirm('Are you sure you want to delete this account?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cuenta.id) }}">
    <button class="btn btn-danger mt-2">Delete</button>
</form>
