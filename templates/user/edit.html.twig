{% extends 'base.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block body %}
	<div class="container pt-5 mt-5 text-white text-center">
		<h1>Edit User</h1>

		{{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
		{{ include('user/_delete_form.html.twig') }}
        <a href="{{ path('app_user_index') }}" class="btn btn-secondary mt-2">Back to users</a>
	</div>
{% endblock %}
